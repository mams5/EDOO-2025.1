#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    
    float num;
    cout<<"Insira um número: ";
    cin>>num;
    float squareRoot;
    squareRoot = sqrt(num);
    cout<<"A raíz quadrada de "<< num<< " é "<<squareRoot<<endl;

    return 0;
    
}
